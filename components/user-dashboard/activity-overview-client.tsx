'use client';

import ActivityOverviewWidget from '@/components/user-dashboard/activity-overview-widget';

const ActivityOverviewClient = () => {
    return <ActivityOverviewWidget />;
};

export default ActivityOverviewClient;